driver:
  image:
    pull_policy: IfNotPresent
    repository: moby/buildkit
    tag: v0.12.5
  replicas: 1
  volumeClaimTemplate:
    accessModes: [ "ReadWriteOnce" ]
    resources:
      requests:
        storage: 25Gi
